import MatchForm from "../MatchForm";
import BasicLayout from "../layouts/BasicLayout";

const HomePage = () => {
  return (
    <BasicLayout>
      <MatchForm />
    </BasicLayout>
  );
};

export default HomePage;
